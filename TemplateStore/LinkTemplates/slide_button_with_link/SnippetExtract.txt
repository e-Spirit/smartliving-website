if (
	lt_link_toggle == "internal",
	if (
		!lt_internal.isEmpty, 
		lt_internal.name, 
		"(no target selected)"
	),
	lt_link_toggle == "external",
	if (
		!lt_url.isEmpty,
		lt_url,
		"(no url defined)"
	),
	lt_link_toggle == "product",
	if (
		!lt_product_link.isEmpty,
		if(!lt_product_link.tt_name.isEmpty(), lt_product_link.tt_name, "(unnamed product)"),
		"(no product selected)"
	),
	"(unknown target)"
)