if("manual".equals(st_type.value) && !st_text.isEmpty(), truncate(st_text.toText(false), 100),
	"product".equals(st_type.value) && !st_product.isEmpty(), st_product.values.get(0).formData.tt_description)